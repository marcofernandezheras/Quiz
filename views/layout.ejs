<!DOCTYPE html>
<html>
<head>
    <title>Preguntas</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link rel="stylesheet" href="/stylesheets/smartphone.css" media="only screen and (max-width: 480px)" />
    <link rel="stylesheet" href="/stylesheets/wide.css" media="only screen and (min-width: 600px) and (min-device-width: 481px)" />
    <link rel="icon" href="favicon.ico" type="image/x-icon" />
</head>
<body>
    <div id="page-wrap">
        <header class="main" id="h1">
            <% if(!session.user) {%>
            <span class="right"><a href="/login">Login</a></span>
            <% } else { %>
            <span class="right"><%=session.user.username%><a href="/logout">Logout</a></span>
            <% } %>
            <h2>Quiz: el juego de las preguntas</h2>
        </header>

        <nav class="main" id="n1">
            <span><a href="/">Inicio</a>
            </span><span><a href="/quizes">Preguntas</a></span>
            </span><span><a href="/author">Autor</a></span>
        </nav>

        <section class="main" id="s1">
            <% if(this.errors && this.errors.length > 0) { %>
            <span id="ErrorMsgs">
                <% for (var i in errors) { %>
                    <span><%= errors[i].message %> <br/></span>
                <% } %>
            </span>
            <% } %>
            <div><%- body %></div>
        </section>

        <footer class="main" id="f1">
            <p><a href="http://es.wikipedia.org/wiki/Quiz">¿Qué es un Quiz?</a></p>
            <p><a href="https://github.com/marcofernandezheras/Quiz/">Fork me on GitHub</a></p>
        </footer>
    </div>
</body>
</html>
